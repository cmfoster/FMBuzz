<% if current_show && @show.id == current_show.id %>
  <p style="font-size:14px;"><%= "<strong>#{message.author}(#{@show.host}): </strong> #{message.msg}".html_safe%></p>
<% else %>
  <p style="font-size:14px;"><%= "<strong>#{message.author}: </strong> #{message.msg}".html_safe%></p>
<% end %>
