<h5 style="text-align:center"><%=@artist.name.truncate(35)%></h5>
<ul class="unstyled"> <!--figure out sorting for songs-->
  <% @artistsongs.sort!{|a1,a2| a1.title.downcase<=>a2.title.downcase}.each do |song|%>
    <li><%= song.title %></li>
  <% end %>
</ul>