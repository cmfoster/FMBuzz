

<div id="chatwall">
</div>

<%= form_for([@show, @message], :remote => true) do |f|%>
  <%= f.text_field :msg %>
  <%= f.hidden_field :show_id, :value => @show.id %>
  <%= f.hidden_field :author, :value => current_show.name %>
  <%= f.submit "send"%>
<% end %>