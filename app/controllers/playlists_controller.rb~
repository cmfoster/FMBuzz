class PlaylistsController < ApplicationController
  
  def create
    @playlist = current_show.playlists.create(params[:playlist])
    @playlist.process
    
    respond_to do |format|
      format.html { redirect_to edit_dj_playlist_path(current_disk_jockey, @playlist) }
    end
  end
  
end
